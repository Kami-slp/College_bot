# 📚 Бот для отчётов по учебной деятельности

Telegram-бот для автоматической обработки Excel-файлов и формирования отчётов по учебной деятельности (ДЗ, посещаемость, расписание и т.д.).

---

## Возможности

| Отчёт | Описание |
|-------|----------|
| 📚 **Домашние задания** | Студенты с % выполненных ДЗ ниже 70% |
| 🧑‍🏫 **Посещаемость преподавателей** | Преподаватели с посещаемостью пар ниже 40% |
| ✅ **Проверка ДЗ** | Преподаватели с % проверенных заданий ниже 70% (месяц и неделя) |
| 📝 **Темы уроков** | Темы, не соответствующие формату «Урок № _. Тема: _» |
| 📅 **Расписание группы** | Количество пар по каждой дисциплине по расписанию на неделю |
| 👨‍🎓 **Отчет по студентам** | Студенты с оценкой за ДЗ = 1 и оценкой за классную работу ≤ 3 |

После обработки бот отправляет превью (первые 5 строк) в чат и полный результат в виде Excel-файла `result.xlsx`.

---

## Требования

- Python 3.8+
- [BotFather](https://t.me/BotFather) — токен Telegram-бота

---

## Установка

1. Клонируйте репозиторий или скопируйте файлы проекта.

2. Создайте виртуальное окружение (по желанию):
   ```bash
   python -m venv venv
   venv\Scripts\activate
   ```

3. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```

4. В `main.py` укажите свой `API_TOKEN` (токен от BotFather):
   ```python
   API_TOKEN = 'ваш_токен_от_BotFather'
   ```

5. Запустите бота:
   ```bash
   python main.py
   ```

---

## Использование

1. Запустите бота командой `/start` в Telegram.
2. Выберите тип отчёта из меню кнопок.
3. Отправьте соответствующий Excel-файл (.xlsx / .xls).
4. Получите превью в чате и файл `result.xlsx` с полным результатом.

**Важно:** Сначала выберите отчёт, затем отправляйте файл. Структура Excel должна соответствовать ожидаемой для выбранного типа отчёта (колонки, заголовки).

---

## Структура проекта

```
PythonProjectg/
├── main.py           # Точка входа, логика бота, обработка сообщений
├── Only_functions.py # Функции обработки Excel (фильтры, отчёты)
├── requirements.txt
└── README.md
```

---

## Зависимости

- **pandas** — чтение и обработка Excel
- **openpyxl** — работа с .xlsx
- **pyTelegramBotAPI** — Telegram Bot API

---

## Безопасность

Не публикуйте `API_TOKEN` в открытых репозиториях. Используйте переменные окружения или секреты:

```python
import os
API_TOKEN = os.getenv('TELEGRAM_BOT_TOKEN', 'ваш_токен')
```

И задавайте `TELEGRAM_BOT_TOKEN` при запуске (например, в `.env` или настройках окружения).
